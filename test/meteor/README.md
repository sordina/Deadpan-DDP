# Test Meteor Applications

In order to run the code in 'test/client' effectively, you should install the meteor 'todos' app.

This isn't checked into version control as it would overwhelm the project.
